{
    "name": "interviewer_website",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "start": "node index",
        "dev": "nodemon index.js",
        "test": "node parser.js",
        "build:ui": "npm i && rm -rf dist && cd ../client && npm i && npm run build && cp -r dist ../server",
        "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
        "postinstall": "mkdir -p uploads"
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "axios": "^1.8.4",
        "cors": "^2.8.5",
        "date-fns": "^3.6.0",
        "dotenv": "^16.4.7",
        "express": "^4.21.2",
        "express-session": "^1.18.1",
        "fs": "^0.0.1-security",
        "http": "^0.0.1-security",
        "mic": "^2.1.2",
        "multer": "^1.4.5-lts.2",
        "openai": "^4.87.3",
        "os": "^0.1.2",
        "socket.io": "^4.8.1",
        "stream": "^0.0.3",
        "uuid": "^10.0.0"
    },
    "devDependencies": {
        "@types/node": "^22.5.0",
        "nodemon": "^3.1.4",
        "vite": "^6.2.5"
    }
}
